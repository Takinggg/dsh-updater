# Security

This plugin installs **only** `@deepseek-ai/dsh` versions published on the npm registry at the `next` or `latest` dist-tags. It never runs sudo and never pipes curl to a shell.

Report vulnerabilities privately via GitHub Security Advisories on this repository. Do not file public issues for exploitable bugs in the update path.
